# サンプル情報
# ハイフンによりリストの要素として格納できる
samples:
  sample1:
    reads:
      R1: "data/sample1_R1.fastq.gz"
      R2: "data/sample1_R2.fastq.gz"
    platform: Illumina
    species: human
    group: control
    library_type: paired-end
    




reference: "ref/genome.fasta"
  # 必要に応じてサンプルを追加



# その他の設定
# configfileに設定すると、全体で共通のパラメータになる
# threads: 8
# output_dir: "QualityCheck"
